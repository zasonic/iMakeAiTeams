# archive/

Historical snapshots that are no longer used by the running app.

| Folder | What it is |
|---|---|
| `legacy-v5/` | The predecessor app (Python + pywebview, single-process). Replaced by the current Electron + FastAPI architecture at the repo root. Kept as a reference; not built, not imported, not packaged. |

The active codebase is at the repository root — see `../README.md`.
