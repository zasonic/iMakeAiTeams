# ── Core / UI ────────────────────────────────────────────────────────────────
pywebview>=4.4.1
requests>=2.31.0
psutil>=5.9.0
numpy>=1.26.0

# ── Anthropic ─────────────────────────────────────────────────────────────────
anthropic>=0.40.0

# ── Resilience ────────────────────────────────────────────────────────────────
tenacity>=8.3.0
pydantic>=2.7.0

# ── Force CPU-only PyTorch to avoid 2GB+ CUDA download ───────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# ── RAG / Embedding / Semantic Search ─────────────────────────────────────────
sentence-transformers>=2.7.0
chromadb>=0.5.0

# ── Priority 2: Hybrid BM25 + vector search ───────────────────────────────────
rank-bm25>=0.2.2

# ── Reranking: FlashRank cross-encoder (4MB, CPU-only, sub-30ms) ─────────────
# Improves RAG precision@K by 10-30% (Pinecone benchmark 2025).
# Optional — app works without it, reranking silently skipped.
flashrank>=0.2.0

# ── Channels: Telegram ────────────────────────────────────────────────────────
python-telegram-bot>=21.0.0

# ── Safety: ML-based security scanners (optional — app works without them) ────
# LLM Guard: prompt injection detection, toxicity, secrets, PII (MIT license)
# Install for ML-based security: pip install llm-guard
# For faster inference: pip install llm-guard[onnxruntime]
# llm-guard>=0.3.0

# NeMo Guardrails: full Colang rail definitions (Apache 2.0)
# Install for programmable dialog rails: pip install nemoguardrails
# nemoguardrails>=0.9.0

# ── Context compression: tree-sitter (optional — fallback to regex) ──────────
# Install for AST-based repo map: pip install tree-sitter tree-sitter-python
# tree-sitter>=0.24.0

# ── Priority 5: Input sanitization (optional — app works without it) ──────────
# llamafirewall>=0.1.0

