# ── Tier 1 (always shipped — lite + full installers) ─────────────────────────
#
# Minimum surface needed for the chat, memory-buffer, router, agents, teams,
# prompt library, security engine, and settings to work end-to-end. Keeps the
# lite bundle under ~60 MB.
#
# Tier 2/3 (RAG/semantic search/telegram) live in requirements-extensions.txt
# and are bundled only by the full installer. All Tier 2/3 imports are lazy,
# so the lite build runs fine without them — service_status() reports them as
# unavailable and the UI degrades gracefully.

# ── Core / UI ────────────────────────────────────────────────────────────────
pywebview>=4.4.1
requests>=2.31.0
psutil>=5.9.0
numpy>=1.26.0
platformdirs>=4.0.0

# ── Anthropic ─────────────────────────────────────────────────────────────────
anthropic>=0.40.0

# ── Resilience ────────────────────────────────────────────────────────────────
tenacity>=8.3.0
pydantic>=2.7.0

# ── OS-native secret storage (DPAPI on Win, Keychain on Mac, SecretService
# on Linux — with a portable file-based fallback when no backend is present).
keyring>=24.0.0
